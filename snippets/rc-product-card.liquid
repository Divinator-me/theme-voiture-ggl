{%- doc -%}
  Carte produit RC — design partagé (landing, collections, recommandations).
{%- enddoc -%}

{% liquid
  assign rating_value = product.metafields.reviews.rating.value.rating
  assign rating_count = product.metafields.reviews.rating_count
  assign color_option = null
  assign on_sale = false

  if product.compare_at_price > product.price
    assign on_sale = true
  endif

  for option in product.options_with_values
    assign option_name = option.name | downcase
    if option_name contains 'couleur' or option_name contains 'color' or option_name contains 'colour'
      assign color_option = option
      break
    endif
  endfor
%}

<article class="rc-product-card rc-product-card--live">
  <div class="rc-product-card__media">
    <div class="rc-product-card__image-wrap">
      {% if on_sale %}
        <span class="rc-product-card__badge">Promo</span>
      {% endif %}

      <a class="rc-product-card__image" href="{{ product.url }}" aria-label="{{ product.title | escape }}">
        {% if product.featured_image %}
          {{
            product.featured_image
            | image_url: width: 700
            | image_tag:
              loading: 'lazy',
              widths: '320, 480, 700',
              sizes: '(min-width: 990px) 28vw, 72vw',
              alt: product.title
          }}
        {% else %}
          <svg viewBox="0 0 260 260" aria-hidden="true">
            <rect width="260" height="260" rx="18" fill="#F4F4F4"/>
            <path d="M54 158c8-31 32-49 72-55l59-8c23-3 44 10 53 31l12 32h-41c-7-17-23-28-42-28s-35 11-42 28H96c-7-17-23-28-42-28-18 0-34 11-41 28h41Z" fill="#0A0A0A"/>
            <circle cx="74" cy="171" r="26" fill="#FF2D2D"/>
            <circle cx="176" cy="171" r="26" fill="#FF2D2D"/>
          </svg>
        {% endif %}
      </a>
    </div>
  </div>

  <div class="rc-product-card__body">
    <h3><a href="{{ product.url }}">{{ product.title }}</a></h3>

    {% if rating_value != blank and rating_count != blank %}
      {% liquid
        assign rating_floor = rating_value | floor
        assign rating_decimal = rating_value | modulo: 1
        assign rating_half = false
        assign rating_next = rating_floor | plus: 1

        if rating_decimal >= 0.3 and rating_decimal <= 0.7
          assign rating_half = true
        elsif rating_decimal > 0.7
          assign rating_floor = rating_floor | plus: 1
          assign rating_next = rating_floor | plus: 1
        endif
      %}
      <div class="rc-product-card__rating" aria-label="Note {{ rating_value }} sur 5, {{ rating_count }} avis">
        <span class="rc-product-card__stars" aria-hidden="true">
          {% for star in (1..5) %}
            {% if star <= rating_floor %}
              <span class="rc-product-card__star is-filled">★</span>
            {% elsif rating_half and star == rating_next %}
              <span class="rc-product-card__star is-half">★</span>
            {% else %}
              <span class="rc-product-card__star">★</span>
            {% endif %}
          {% endfor %}
        </span>
        <span class="rc-product-card__reviews">({{ rating_count }})</span>
      </div>
    {% endif %}

    <p class="rc-product-card__price">
      {{ product.price | money }}
      {% if on_sale %}
        <span class="rc-product-card__price-compare"><s>{{ product.compare_at_price | money }}</s></span>
      {% endif %}
    </p>

    {% if color_option != null and color_option.values.size > 0 %}
      <ul class="rc-product-card__colors" aria-label="Couleurs disponibles">
        {% for value in color_option.values %}
          <li>
            {% render 'rc-color-swatch', product: product, color_option: color_option, value: value %}
          </li>
        {% endfor %}
      </ul>
    {% endif %}
  </div>
</article>
